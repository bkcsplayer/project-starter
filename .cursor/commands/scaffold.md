# scaffold
Create a minimal runnable prototype using this repo structure.
- Web: Vite React TS + Tailwind (apps/web)
- Admin: React-Admin starter (apps/admin)
- API: pick Node by default (services/api-node), keep Go/Py untouched
- Ensure docker compose builds and runs successfully.
- Update README with any changes.
